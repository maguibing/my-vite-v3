import {
    create, 
    NButton, 
    NSpace,
    NMessageProvider
} from 'naive-ui'

export const naive = create({
    components:[
        NButton,
        NSpace,
        NMessageProvider
    ]
})

